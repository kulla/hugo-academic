---
- id: title_head
  translation: "{{ if not .IsHome }}{{ .Title }} | {{ end }}{{ .Site.Title }}"

- id: rss_feed_name
  translation: "{{ .Site.Title }}"

- id: toggle_navigation
  translation: Toggle navigation
